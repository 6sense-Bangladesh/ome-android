<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.inirv">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".iNirvApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:name">
<!--        <activity-->
<!--            android:name=".CreateAccountActivity"-->
<!--            android:exported="false" />-->
        <activity
            android:name=".AppLevel.AppLevelActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Activities -->
<!--        <activity android:name=".LoginAndCreateAccount.Welcome.WelcomeActivity" />-->
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginAndCreateAccount.LoginActivity" />
        <activity android:name=".LoginAndCreateAccount.SetupAccountActivity" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
        </activity>
        <activity android:name=".Knob.KnobActivity" />
        <activity android:name=".Menu.MenuActivity" />
        <activity android:name=".Settings.SettingsActivity" />
    </application>

</manifest>