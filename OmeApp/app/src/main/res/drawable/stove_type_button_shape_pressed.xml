<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="360"
        android:centerColor="@color/gradient_mid_unpressed_color"
        android:endColor="@color/gradient_end_unpressed_color"
        android:startColor="@color/gradient_start_unpressed_color" />
    <stroke android:width="1dp" android:color="@color/gray_color" />
</shape>
